#include "list.h"
/**
 * print_list - print all the element of a lsit_t list.
 * @h: header passed
 * Return: number of nodes.
 */
size_t print_list(const list_t *h)
{
	int count;

	while (s != NULL)
	{
		h->next;
		count++;
	}
	return (count);
}
