#include "main.h"
/**
 * mul - multiplies two integers
 *
 * @a: first num pass argument.
 * @b: second num pass argumnt.
 *
 * Return: multiple of two number.
 */
int mul(int a, int b)
{
	return (a * b);
}
